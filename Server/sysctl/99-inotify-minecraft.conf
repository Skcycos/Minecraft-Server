# 食韵筑家：提高 inotify 限额
# 解决 NeoForge 模组加载配置时：
#   User limit of inotify instances reached or too many open files
#   （常见于 Ecliptic Seasons 等模组注册 config FileWatcher 失败）
#
# 默认 max_user_instances=128，在 KDE + 客户端 + 服务端同时运行时容易耗尽。
fs.inotify.max_user_instances = 512
fs.inotify.max_user_watches = 524288
