/**
 * Public API of TCTH Integration.
 *
 * <p><b>Stability statement:</b> TCTH is currently in pre-release (0.x).
 * Everything in this package and its subpackages is considered <em>unstable</em>:
 * classes, methods and constants may be added, changed, moved or removed without
 * notice until version 1.0.0. Anything outside this package is internal
 * implementation ({@code com.tanrunn.tcth.impl}) and must not be referenced.
 *
 * <p>Integrators should pin the exact TCTH version they build against and
 * re-verify the API on every upgrade while TCTH is below 1.0.0.
 */
package com.tanrunn.tcth.api;
